
Translation results
Translation result
The web site has first a home page. The home page has a navbar, banner, list of properties and a footer.
Here are some animations used in property lists.

The navbar contains the site name, some links and a profile image.
The profile option will show in the navbar if the user is logged in.
And clicking on the profile image will open a menubar.
From now user can log out and go to profile page.

Clicking on the View Property button on the property list cards will open the property detail page, but the user must be logged in. Otherwise, it will go to the login page.

Footer will be visible on all pages.

  There is an about us page and it includes private

Login page has login form with option to login with google and twitter.

The register page contains the register form.

On the profile page, the user can view and edit his profile details.